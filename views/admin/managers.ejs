<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        :root {
            --background: #ffffff;
            --foreground: #0a0a0a;
            --card: #ffffff;
            --card-foreground: #0a0a0a;
            --primary: #030213;
            --primary-foreground: #ffffff;
            --secondary: #f1f1f3;
            --secondary-foreground: #030213;
            --muted: #ececf0;
            --muted-foreground: #717182;
            --accent: #e9ebef;
            --accent-foreground: #030213;
            --destructive: #d4183d;
            --destructive-foreground: #ffffff;
            --border: rgba(0, 0, 0, 0.1);
            --input: #ffffff;
            --radius: 0.625rem;
            --success: #22c55e;
            --warning: #f59e0b;
        }

        * {
            box-sizing: border-box;
        }

        body {
            background: var(--background);
            color: var(--foreground);
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
            line-height: 1.6;
            margin: 0;
            min-height: 100vh;
        }

        .modern-nav {
            background: var(--card);
            border-bottom: 1px solid var(--border);
            padding: 1rem 0;
            position: sticky;
            top: 0;
            z-index: 1000;
        }

        /* Navigation Styles */
        .nav-bar {
            background: var(--card);
            border-bottom: 1px solid var(--border);
            padding: 1rem 0;
        }

        .nav-container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 1rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .nav-left {
            display: flex;
            align-items: center;
            gap: 2rem;
        }

        .nav-brand {
            font-size: 1.125rem;
            font-weight: 500;
            color: var(--foreground);
            text-decoration: none;
        }

        .nav-links {
            display: flex;
            gap: 1rem;
        }

        .nav-link {
            padding: 0.375rem 0.75rem;
            border-radius: calc(var(--radius) - 2px);
            text-decoration: none;
            color: var(--foreground);
            transition: background-color 0.2s ease;
            font-size: 0.875rem;
        }

        .nav-link.active {
            background: var(--primary);
            color: var(--primary-foreground);
        }

        .nav-link:hover:not(.active) {
            background: var(--accent);
            color: var(--accent-foreground);
        }

        .nav-right {
            display: flex;
            align-items: center;
            gap: 1rem;
        }

        .nav-user {
            font-size: 0.875rem;
            color: var(--muted-foreground);
        }

        .nav-button {
            background: var(--secondary);
            color: var(--secondary-foreground);
            border: 1px solid var(--border);
            padding: 0.375rem 0.75rem;
            border-radius: calc(var(--radius) - 2px);
            font-size: 0.875rem;
            cursor: pointer;
            transition: all 0.2s ease;
        }

        .nav-button:hover {
            background: var(--accent);
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 2rem 1rem;
        }

        .page-header {
            margin-bottom: 2rem;
        }

        .page-title {
            font-size: 1.5rem;
            font-weight: 600;
            color: var(--foreground);
            margin: 0 0 0.5rem 0;
        }

        .page-description {
            color: var(--muted-foreground);
            font-size: 0.875rem;
            margin: 0;
        }

        .page-header {
            margin-bottom: 2rem;
        }

        .page-title {
            font-size: 2rem;
            font-weight: 600;
            color: var(--foreground);
            margin: 0 0 0.5rem 0;
        }

        .page-description {
            color: var(--muted-foreground);
            font-size: 1rem;
            margin: 0 0 1rem 0;
        }

        .breadcrumb {
            background: var(--card);
            padding: 1rem;
            margin: 0 0 1rem 0;
            font-size: 0.875rem;
            border: 1px solid var(--border);
            border-radius: var(--radius);
            box-shadow: 0 1px 3px 0 rgba(0, 0, 0, 0.1), 0 1px 2px 0 rgba(0, 0, 0, 0.06);
        }

        .breadcrumb-item a {
            color: var(--primary);
            text-decoration: none;
            font-weight: 500;
        }

        .breadcrumb-item a:hover {
            text-decoration: underline;
        }

        .breadcrumb-item.active {
            color: var(--muted-foreground);
        }

        .breadcrumb-item + .breadcrumb-item::before {
            content: ">";
            padding: 0 0.5rem;
            color: var(--muted-foreground);
        }

        .back-button {
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
            color: var(--primary);
            text-decoration: none;
            font-size: 0.875rem;
            font-weight: 500;
            margin-bottom: 1rem;
            padding: 0.5rem 1rem;
            border: 1px solid var(--border);
            border-radius: calc(var(--radius) - 2px);
            background: var(--card);
            transition: all 0.2s ease;
        }

        .back-button:hover {
            background: var(--accent);
            text-decoration: none;
        }

        .dashboard-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 1.5rem;
            margin-bottom: 2rem;
        }

        .card {
            background: var(--card);
            color: var(--card-foreground);
            border: 1px solid var(--border);
            border-radius: var(--radius);
            overflow: hidden;
            box-shadow: 0 1px 3px 0 rgba(0, 0, 0, 0.1), 0 1px 2px 0 rgba(0, 0, 0, 0.06);
        }

        .card-header {
            padding: 1.5rem 1.5rem 0 1.5rem;
            border: none;
            background: transparent;
        }

        .card-title {
            font-size: 1.125rem;
            font-weight: 600;
            line-height: 1.2;
            margin: 0 0 0.5rem 0;
            color: var(--card-foreground);
        }

        .card-description {
            color: var(--muted-foreground);
            font-size: 0.875rem;
            margin: 0;
        }

        .card-content {
            padding: 1.5rem;
        }

        .form-label {
            font-weight: 500;
            color: var(--foreground);
            margin-bottom: 0.5rem;
        }

        .form-control {
            background: var(--input);
            border: 1px solid var(--border);
            border-radius: calc(var(--radius) - 2px);
            padding: 0.75rem;
            color: var(--foreground);
            transition: all 0.2s ease;
        }

        .form-control:focus {
            outline: none;
            border-color: var(--primary);
            box-shadow: 0 0 0 3px rgba(3, 2, 19, 0.1);
        }

        .form-text {
            color: var(--muted-foreground);
            font-size: 0.75rem;
        }

        .btn {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            white-space: nowrap;
            border-radius: calc(var(--radius) - 2px);
            font-size: 0.875rem;
            font-weight: 500;
            transition: all 0.2s ease;
            outline: none;
            text-decoration: none;
            border: 1px solid transparent;
            cursor: pointer;
            padding: 0.75rem 1rem;
            gap: 0.5rem;
        }

        .btn-primary {
            background: var(--primary);
            color: var(--primary-foreground);
        }

        .btn-primary:hover {
            background: rgba(3, 2, 19, 0.9);
            color: var(--primary-foreground);
            text-decoration: none;
        }

        .btn-destructive {
            background: var(--destructive);
            color: var(--destructive-foreground);
        }

        .btn-destructive:hover {
            background: rgba(212, 24, 61, 0.9);
            color: var(--destructive-foreground);
            text-decoration: none;
        }

        .alert {
            border-radius: calc(var(--radius) - 2px);
            border: 1px solid transparent;
            padding: 1rem;
            margin-bottom: 1rem;
            font-size: 0.875rem;
        }

        .alert-success {
            background: rgba(34, 197, 94, 0.1);
            color: var(--success);
            border-color: rgba(34, 197, 94, 0.2);
        }

        .alert-destructive {
            background: rgba(212, 24, 61, 0.1);
            color: var(--destructive);
            border-color: rgba(212, 24, 61, 0.2);
        }

        .alert-warning {
            background: rgba(245, 158, 11, 0.1);
            color: var(--warning);
            border-color: rgba(245, 158, 11, 0.2);
        }

        .table-container {
            border: 1px solid var(--border);
            border-radius: var(--radius);
            overflow: hidden;
        }

        .table {
            margin: 0;
            width: 100%;
            border-collapse: collapse;
        }

        .table th {
            background: var(--muted);
            color: var(--foreground);
            font-weight: 600;
            padding: 0.75rem 1rem;
            text-align: left;
            border-bottom: 1px solid var(--border);
        }

        .table td {
            padding: 0.75rem 1rem;
            border-bottom: 1px solid var(--border);
        }

        .table tbody tr:hover {
            background: var(--muted);
        }

        .table tbody tr:last-child td {
            border-bottom: none;
        }

        .badge {
            display: inline-flex;
            align-items: center;
            padding: 0.25rem 0.5rem;
            border-radius: calc(var(--radius) - 4px);
            font-size: 0.75rem;
            font-weight: 500;
        }

        .badge-primary {
            background: var(--primary);
            color: var(--primary-foreground);
        }

        .empty-state {
            text-align: center;
            padding: 3rem 1rem;
            color: var(--muted-foreground);
        }

        .empty-state i {
            font-size: 3rem;
            margin-bottom: 1rem;
            opacity: 0.5;
        }

        .space-y-4 > * + * {
            margin-top: 1rem;
        }

        .space-y-2 > * + * {
            margin-top: 0.5rem;
        }

        @media (max-width: 768px) {
            .dashboard-grid {
                grid-template-columns: 1fr;
            }
            
            .container {
                padding: 1rem;
            }
        }
    </style>
</head>
<body>
    <!-- Navigation -->
        <!-- Navigation -->
    <nav class="nav-bar">
        <div class="nav-container">
            <div class="nav-left">
                <a href="/dashboard" class="nav-brand">SecureTask</a>
                <div class="nav-links">
                    <a href="/dashboard" class="nav-link">Dashboard</a>
                    <a href="/admin/managers" class="nav-link active">Manage Managers</a>
                    <a href="/admin/logs" class="nav-link">System Logs</a>
                    <a href="/account/change-password" class="nav-link">Change Password</a>
                </div>
            </div>
            <div class="nav-right">
                <span class="nav-user"><%= user.username %> (Administrator)</span>
                <form method="POST" action="/logout" style="margin: 0;">
                    <button type="submit" class="nav-button">Logout</button>
                </form>
            </div>
        </div>
    </nav>

    <!-- Main Content -->
    <div class="container">
        <!-- Page Header -->
        <div class="page-header">
            <h1 class="page-title">Manage Project Managers</h1>
            <p class="page-description">
                Create and manage Project Manager accounts for task delegation and team management.
            </p>
        </div>

        <!-- Flash Messages -->
        <% if (typeof validationErrors !== 'undefined' && validationErrors.length > 0) { %>
            <div class="alert alert-destructive">
                <ul style="margin: 0; padding-left: 1rem;">
                    <% validationErrors.forEach(error => { %>
                        <li><%= error.msg %></li>
                    <% }) %>
                </ul>
            </div>
        <% } %>

        <% if (typeof successMessage !== 'undefined' && successMessage) { %>
            <div class="alert alert-success">
                <i class="fas fa-check-circle me-2"></i>
                <%= successMessage %>
            </div>
        <% } %>

        <!-- Dashboard Cards -->
        <div class="dashboard-grid">
            <!-- Create New Manager Card -->
            <div class="card">
                <div class="card-header">
                    <h2 class="card-title">Create Project Manager Account</h2>
                    <p class="card-description">
                        Create a new account for a Project Manager.
                    </p>
                </div>
                <div class="card-content">
                    <form method="POST" action="/admin/create-manager" class="space-y-4">
                        <div class="space-y-2">
                            <label for="username" class="form-label">Username *</label>
                            <input type="text" 
                                   class="form-control" 
                                   id="username" 
                                   name="username" 
                                   value="<%= typeof formData !== 'undefined' ? formData.username || '' : '' %>"
                                   required 
                                   maxlength="30"
                                   pattern="[a-zA-Z0-9_]+"
                                   title="Only letters, numbers, and underscores allowed"
                                   placeholder="Enter username">
                            <p class="form-text">3-30 characters, letters, numbers, and underscores only</p>
                        </div>
                        
                        <div class="space-y-2">
                            <label for="password" class="form-label">Password *</label>
                            <input type="password" 
                                   class="form-control" 
                                   id="password" 
                                   name="password" 
                                   required 
                                   minlength="8"
                                   maxlength="128"
                                   placeholder="Enter secure password">
                            <p class="form-text">
                                At least 8 characters with uppercase, lowercase, number, and special character
                            </p>
                        </div>
                        
                        <button type="submit" class="btn btn-primary" style="width: 100%;">
                            <i class="fas fa-user-plus me-2"></i>Create Manager
                        </button>
                    </form>
                </div>
            </div>

            <!-- Existing Managers Card -->
            <div class="card">
                <div class="card-header">
                    <h2 class="card-title">Project Manager Accounts</h2>
                    <p class="card-description">
                        Manage existing Project Manager accounts.
                        <span class="badge badge-primary" style="margin-left: 0.5rem;"><%= managers.length %> manager(s)</span>
                    </p>
                </div>
                <div class="card-content">
                    <% if (managers.length === 0) { %>
                        <div class="empty-state">
                            <i class="fas fa-users"></i>
                            <p>No Project Manager accounts exist yet.</p>
                            <p style="font-size: 0.875rem;">Create the first one using the form on the left.</p>
                        </div>
                    <% } else { %>
                        <div class="table-container">
                            <table class="table">
                                <thead>
                                    <tr>
                                        <th>Username</th>
                                        <th>Created</th>
                                        <th>Actions</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    <% managers.forEach(manager => { %>
                                        <tr>
                                            <td>
                                                <div style="display: flex; align-items: center; gap: 0.5rem;">
                                                    <i class="fas fa-user-tie" style="color: var(--primary);"></i>
                                                    <strong><%= manager.username %></strong>
                                                </div>
                                            </td>
                                            <td>
                                                <span style="color: var(--muted-foreground); font-size: 0.875rem;">
                                                    <%= new Date(manager.created_at).toLocaleDateString() %>
                                                </span>
                                            </td>
                                            <td>
                                                <form method="POST" action="/admin/delete-manager" class="d-inline">
                                                    <input type="hidden" name="managerId" value="<%= manager.id %>">
                                                    <button type="submit" 
                                                            class="btn btn-destructive" 
                                                            style="padding: 0.5rem 0.75rem; font-size: 0.75rem;"
                                                            data-confirm-delete 
                                                            title="Delete Manager Account">
                                                        <i class="fas fa-trash me-1"></i>Delete
                                                    </button>
                                                </form>
                                            </td>
                                        </tr>
                                    <% }) %>
                                </tbody>
                            </table>
                        </div>
                    <% } %>
                </div>
            </div>
        </div>

        <!-- Security Warning -->
        <div class="card">
            <div class="card-content">
                <div class="alert alert-warning" style="margin: 0;">
                    <i class="fas fa-exclamation-triangle me-2"></i>
                    <strong>Important:</strong> 
                    Deleting a Project Manager account is permanent and cannot be undone. 
                    Any tasks created by the deleted manager will remain in the system but cannot be managed by them.
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        // Confirm delete actions
        document.addEventListener('DOMContentLoaded', function() {
            var deleteButtons = document.querySelectorAll('[data-confirm-delete]');
            deleteButtons.forEach(function(button) {
                button.addEventListener('click', function(e) {
                    if (!confirm('Are you sure you want to delete this manager account? This action cannot be undone.')) {
                        e.preventDefault();
                    }
                });
            });
        });
    </script>
</body>
</html>
